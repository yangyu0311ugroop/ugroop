import { SIZE_CONSTANTS } from 'sizeConstants';

const style = {
  rte: {
    overflow: 'hidden',
    margin: '8px 4px 0px',
  },
  [`${SIZE_CONSTANTS.XXS}Width`]: {
    width: 340,
  },
  descriptionContainer: {
    maxWidth: '100%',
  },
  compact: {
    maxHeight: 40,
    overflow: 'hidden',
  },
};

export default style;
