import React from 'react';
import JText from 'components/JText';

export const NoParticipantPlaceholder = () => (
  <JText>No participants in this group</JText>
);

NoParticipantPlaceholder.propTypes = {};

export default NoParticipantPlaceholder;
