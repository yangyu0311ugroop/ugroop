import { defineMessages } from 'react-intl';

export default defineMessages({
  label: {
    id: 'app.smartComponents.Organisation.parts.CreatedDate.label',
    defaultMessage: 'Created Date',
  },
});
