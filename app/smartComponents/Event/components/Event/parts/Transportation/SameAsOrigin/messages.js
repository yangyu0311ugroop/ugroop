import { defineMessages } from 'react-intl';

export default defineMessages({
  label: {
    id:
      'app/smartComponents/Event/components/Event/parts/Common/SameAsOrigin/messages.js',
    defaultMessage: 'Same as origin',
  },
});
