import { defineMessages } from 'react-intl';

export default defineMessages({
  infoLabel: {
    id: 'app.Event.parts.Event.BatchCreate.infoLabel',
    defaultMessage:
      'An activity will be created on each day within the range specified above and below.',
  },
});
