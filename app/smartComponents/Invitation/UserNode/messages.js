import { defineMessages } from 'react-intl';

export default defineMessages({
  rowDescription: {
    id: 'app.Invitation.UserNode.rowDescription',
    defaultMessage: 'Assigned {from} {at}.',
  },
});
