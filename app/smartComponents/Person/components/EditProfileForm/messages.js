import { defineMessages } from 'react-intl';

export default defineMessages({
  submitBtn: {
    id: 'app.smartComponents.Person.components.EditProfileForm.submitBtn',
    defaultMessage: 'Save',
  },
});
