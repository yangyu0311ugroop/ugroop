const styles = ({ colors }) => ({
  lineIndicator: {
    width: 40,
    height: 2,
    backgroundColor: colors.base,
  },
  name: {
    marginTop: 8,
  },
  noWordBreak: {
    wordBreak: 'normal',
  },
});

export default styles;
