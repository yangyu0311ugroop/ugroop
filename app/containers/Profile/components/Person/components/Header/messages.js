import { defineMessages } from 'react-intl';

export default defineMessages({
  joinedLabel: {
    id:
      'app.containers.Profile.components.Person.components.Header.joinedLabel',
    defaultMessage: 'Joined last ',
  },
});
