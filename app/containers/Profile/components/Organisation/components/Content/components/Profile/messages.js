import { defineMessages } from 'react-intl';

export default defineMessages({
  sample: {
    id:
      '.app.containers.Profile.components.Organisation.components.Content.Profile',
    defaultMessage: 'This is the Profile component !',
  },
});
