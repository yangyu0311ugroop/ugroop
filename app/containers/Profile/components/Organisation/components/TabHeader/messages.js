import { defineMessages } from 'react-intl';

export default defineMessages({
  sample: {
    id: '.app.containers.Profile.components.Organisation',
    defaultMessage: 'This is the TabHeader component !',
  },
});
