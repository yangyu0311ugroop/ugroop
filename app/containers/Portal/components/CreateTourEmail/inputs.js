import m from './mesage';

const TITLE = {
  autoFocus: true,
  required: true,
  autoComplete: 'off',
  name: 'content',
  placeholder: m.email.defaultMessage,
  variant: 'outlined',
};

export default {
  TITLE,
};
