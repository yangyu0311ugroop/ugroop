import { defineMessages } from 'react-intl';

export default defineMessages({
  deleteDescription: {
    id: 'app.containers.Portal.DeleteChannel.description',
    defaultMessage:
      'Deleting a chat group will permanently remove all of its messages. This cannot be undone.',
  },
});
