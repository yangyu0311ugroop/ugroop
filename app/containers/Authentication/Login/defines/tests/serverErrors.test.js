/**
 * Created by quando on 21/3/17.
 */

import { INVALID_CREDENTIALS } from '../serverErrors';

describe('defines/serverErrors ', () => {
  it('should have INVALID_CREDENTIALS', () => {
    expect(INVALID_CREDENTIALS);
  });
});
