/**
 * Created by quando on 21/3/17.
 */

import { EMAIL, PASSWORD } from '../inputs';

describe('defines/inputs ', () => {
  it('should have EMAIL', () => {
    expect(EMAIL);
  });
  it('should have PASSWORD', () => {
    expect(PASSWORD);
  });
});
