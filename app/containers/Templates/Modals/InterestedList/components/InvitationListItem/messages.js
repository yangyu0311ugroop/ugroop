import { defineMessages } from 'react-intl';

export default defineMessages({
  subtitleCreatedAt: {
    id:
      'app.Containers.Templates.Modals.InterestedList.InvitationListItem.subtitleCreatedAt',
    defaultMessage: 'Sent {at}',
  },
});
