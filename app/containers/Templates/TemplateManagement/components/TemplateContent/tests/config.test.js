import { CONFIG } from '../config';

describe('TemplateContent/tests/config.test.js', () => {
  describe('CONFIG', () => {
    it('should exists', () => {
      expect(CONFIG).toBeDefined();
    });
  });
});
