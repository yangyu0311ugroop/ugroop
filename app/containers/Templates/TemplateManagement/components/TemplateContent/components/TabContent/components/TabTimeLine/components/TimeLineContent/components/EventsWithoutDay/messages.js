import { defineMessages } from 'react-intl';

const m = defineMessages({
  title: {
    id: 'app.containers.Template.TimeLineContent.EventsWithoutDay.title',
    defaultMessage: 'To be planned',
  },
});

export default m;
