import { CONFIG } from '../config';

describe('TAB_TIMELINE_CONTENT Config', () => {
  describe('config', () => {
    it('should exists', () => {
      expect(CONFIG).toBeDefined();
    });
  });
});
