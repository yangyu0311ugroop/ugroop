import { defineMessages } from 'react-intl';

export default defineMessages({
  error: {
    id: 'app.containers.Templates.Components.TemplateBreadcrumbs.error',
    defaultMessage: "Something wrong happened. Can't render the breadcrumb. :(",
  },
});
