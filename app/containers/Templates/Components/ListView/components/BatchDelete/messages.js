import { defineMessages } from 'react-intl';

export default defineMessages({
  countText: {
    id:
      'app.containers.Templates.Components.ListView.components.BatchDelete.sample',
    defaultMessage: 'selected',
  },
});
