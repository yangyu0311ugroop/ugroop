import { MY_TEMPLATES_DATASTORE } from 'appConstants';

export const CONFIG = {
  value: {
    currFolderId: [MY_TEMPLATES_DATASTORE, 'id'],
  },
  setValue: {
    folderFormOpen: [MY_TEMPLATES_DATASTORE, 'folderFormOpen'],
  },
};
