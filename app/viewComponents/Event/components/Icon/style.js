/**
 * Created by stephenkarpinskyj on 27/6/18.
 */

const style = {
  bold: {
    fontWeight: 800,
  },

  walkin: {
    marginTop: 12,
  },
  walkout: {
    marginTop: 12,
  },
  // Since the bicycle icon is different to other icons we have in events,
  // we would be adding a special line height design for bicycle.
  // Probably the best move for this one is for us to ask for a custom icon
  // design just like in other transportation we have
  cyclingLineHeight: {
    lineHeight: 0.5,
  },
};

export default style;
